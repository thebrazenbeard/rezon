# Rezon P0 Source Review ? 2026-09-15

Status: SOURCE STUDY / ARCHITECTURAL INPUT / NO RUNTIME AUTHORITY

This review records mechanisms that may influence Rezon. Repository names and benchmark claims are not authority. Kernel V0 stays deterministic and provider-independent; learned/latent/path-confidence signals remain advisory until separately benchmarked.

## Highest-value donors

- **HCAE** ? exact reviewed subject `basiralab/HCAE@ed13937a5266aadbfa34cd57e6c9a703470c77e9`. Transfers coherent shared-carrier multi-view hypergraph construction, higher-order relations, and an optional learned structural encoder. Its TensorFlow 1.x implementation is not a kernel dependency; learned geometry cannot establish identity/evidence/authority.
- **KAG** ? transfers logical-form-guided operator composition across exact/text retrieval, graph/semantic reasoning, language reasoning, and numerical calculation. Rezon should borrow operator interfaces and planning concepts, not the heavyweight deployment.
- **Semantica** ? transfers first-class provenance, deterministic rule paths, conflict/snapshot/decision records, and system-level explainability. Useful for receipt semantics; not a reason to import its whole storage stack.
- **Ontosphere** ? transfers canonical semantic-state hashing, explicit inferred-vs-asserted state, SHACL/OWL verification, diagnosis, verified repair, edit provenance, and reversal. Strong verifier/repair donor.
- **Zelph** ? transfers executable-graph and derivation/proof-chain concepts. AGPL/commercial licensing and scope make it research input rather than Kernel V0 dependency.
- **PageIndex** ? transfers hierarchical structural retrieval and reasoning-guided relevance navigation as an alternative/control to vector similarity. Its published benchmark claims must be re-tested on Rezon corpora.

## Routing and reasoning-control donors

- **HRM** ? transfers the semantic principle of slow/abstract coordination plus fast/detail computation. The recurrent neural architecture is not an agent scheduler.
- **HyPER** ? exact reviewed tree `50250a7b58eddd189a9a4c3c3cefe1621a06ed96`. Its scheduler expands/reduces hypothesis paths under token budget using confidence/diversity/entropy/novelty signals. Those are valuable post-kernel routing experiments but are never evidence or authority.
- **custom-reverse-proxy** ? supplies round-robin, weighted, metadata, ML, and ML-weighted selector baselines plus metric caches. Rezon can use these as routing-control baselines; security/deployment assumptions do not transfer.
- **SGR tool calling** ? transfers bounded iterations/search/clarification, explicit plan/adapt/final states, and tool inventory. License evidence is conflicting; reference-only until resolved.

## Representation / diagnostic research

- **HyperbolicReasoning** ? exact reviewed tree `809a7f1d8dcbaa14233298e371edadc86029979c`; provides Poincare-ball operations and hierarchical symbolic explanation geometry. Possible alternate structural representation after kernel acceptance.
- **hyperbolic-reasoning-probe** ? provides Euclidean-vs-hyperbolic structural probes for LLM hidden states and layer/token ablations. Diagnostic benchmark idea, not a canonical state substrate.
- **connectome_fingerprint** ? provides ridge-prediction, network-removal ablation, and permutation-test patterns. Strong evaluation-method donor, weak runtime donor.
- **DRISHTE-Public** ? transfers the semantic distinction `observation_t != persistent track`; implementation details are partly redacted, so no undocumented re-identification mechanism is inferred.

## Low-priority / bounded references

- **ZEEJAI-Hyper-Chat** ? SFT ? GRPO ? agentic-RAG training/evaluation reference. Useful later for learned executor evaluation; not hyperbolic geometry and not Kernel V0.
- **Program_Tool_For_AI_Connectome_Dome** ? currently a single browser visualization artifact with explicit/inferred hierarchy links and nested graph spaces. Future UX inspiration only; inferred visualization links must never become epistemic evidence.
- **Binomial-Heterogenicity** ? repeated stochastic simulation methodology only; no architectural dependency.

## P0 consequence

The source review does not justify adding more frameworks to Kernel V0. It strengthens four requirements instead: explicit provenance/receipts, real independence/contamination metadata, deterministic operator/routing baselines, and hostile ablation/permutation/broken-reference evaluation. HCAE/HyPER/hyperbolic signals become post-kernel experiments competing against the deterministic baseline.
